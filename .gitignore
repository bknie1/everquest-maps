# Release artifact -- built with Compress-Archive from "Emoda Legends Maps",
# attached to GitHub releases, never committed.
maps.zip

# Local render output from src/tools/render_zone.py
renders/

# Fable's thinking/reasoning doc - kept locally as a build guide, not tracked in source.
src/docs/BRAIN.md
